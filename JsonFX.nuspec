<?xml version="1.0"?>
<package>
  <metadata>
    <!-- The identifier that must be unique within the hosting gallery -->
    <id>SpaceApe.JsonFx</id>

    <!-- The package version number that is used when resolving dependencies -->
    <version>1.0.5</version>

    <!-- Authors contain text that appears directly on the gallery -->
    <authors>SpaceApeGames</authors>

    <!-- Owners are typically nuget.org identities that allow gallery 
         users to earily find other packages by the same owners.  -->
    <owners>sy</owners>

    <!-- License and project URLs provide links for the gallery -->
    <projectUrl>https://github.com/spaceapegames/jsonfx-for-unity3d</projectUrl>

    <!-- If true, this value prompts the user to accept the license when
         installing the package. -->
    <requireLicenseAcceptance>false</requireLicenseAcceptance>

    <!-- Any details about this particular release -->
    <releaseNotes>Support for scanning assemblies for type coercision (for loading polymorphic data)</releaseNotes>

    <!-- The description can be used in package manager UI. Note that the 
         nuget.org gallery uses information you add in the portal. -->
    <description>JsonFX for Unity</description>

    <!-- Copyright information -->
    <copyright>Copyright ©2016 Space Ape Games</copyright>

    <!-- Tags appear in the gallery and can be used for tag searches -->
    <tags>unity dll c# nuget, json jsonfx</tags>

    <dependencies>
      <group targetFramework=".NETFramework3.5" />
      <group targetFramework=".NETStandard2.0" />
    </dependencies>
  </metadata>

  <files>
    <file src="JsonFx/JsonFx.Json/bin/Release/lib/net35/JsonFx.Json.dll" target="lib/net35" />
    <file src="JsonFx/JsonFx.Json/bin/Release/lib/net35/JsonFx.Json.*db" target="lib/net35" />
    <file src="JsonFx/JsonFx.Json/bin/Release/lib/netstandard2.0/JsonFx.Json.dll" target="lib/netstandard2.0" />
    <file src="JsonFx/JsonFx.Json/bin/Release/lib/netstandard2.0/JsonFx.Json.*db" target="lib/netstandard2.0" />
    <file src="JsonFx/JsonFx.Json/bin/Release/JsonFx.Json.XML" />
  </files>
</package>